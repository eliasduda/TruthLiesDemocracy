using UnityEngine;

public enum EventCategory
{
    Other,
    Engagement,
    Awareness,
    Funding
}

public enum InfluencableStats
{
    Alignment,
    Engagement,
    MoneyPerDay,
    MoneyTotal,
    DaysPassed
}

public enum StatComparator
{
    GreaterThan,
    LessThan,
    EqualTo
}