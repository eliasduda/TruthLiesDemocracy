using UnityEngine;

public enum EventCategory
{
    Other,
    Engagement,
    Awareness,
    Funding
}

public enum InfluencableStats
{
    Alignment,
    Engagement,
    MoneyPerSecond,
    MoneyTotal,
}

public enum StatComparator
{
    GreaterThan,
    LessThan,
    EqualTo
}